<%= stylesheet_pack_tag 'Master' %>
<%= javascript_pack_tag 'Master' %>

<%= content_tag :div,
                id: "master",
                data: {
                    authToken: current_user ? User.create_token("web", current_user.id) : nil,
                    name: current_user&.full_name,
                    isAdmin: current_user&.admin?,
                    orgName: ENV["ORG_NAME"]
                }.to_json do %>
<% end %>