<%= stylesheet_pack_tag 'Master' %>
<%= javascript_pack_tag 'Master' %>

<%= content_tag :div,
                id: "master",
                data: {
                    authToken: current_user ? get_auth_token : nil,
                    name: current_user&.full_name,
                    isAdmin: current_user&.admin?,
                    orgName: @org_name,
                    orgConfig: @config
                }.to_json do %>
<% end %>
